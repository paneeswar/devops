git pull --rebase --- Git pull rebase is a method of combining your local unpublished changes with the latest published changes on your remote 

rebasing. If you pull remote changes with the flag --rebase , then your local changes are reapplied on top of the remote changes. ...
merging. If you pull remote changes with the flag --merge , which is also the default, then your local changes are merged with the remote changes.

git remote add orgin url -- to map the repo 
git add .
git commit filename -m " "
git commit -a -m " "
git status
git log 
git log -2
git checkout master switching baranches 

